---
title: Panel de Administración - Centro de Moderación Completo
description: Construye panel de administración listo para producción con estadísticas en tiempo real, gráficos, feed de actividad, acciones rápidas y registros de auditoría. Domina la administración de foros.
---

# Advertencia

:::u-alert
---
title: Los comentarios en el código son solo para fines educativos.
description: En proyectos reales, evita agregar comentarios irrelevantes o redundantes.
color: warning
variant: subtle
icon: i-lucide-triangle-alert
---
:::

El panel de administración proporciona una vista general de la salud del foro y acceso rápido a herramientas de moderación.

---

## Componente del Panel

```tsx
// src/pages/admin/AdminDashboard.tsx

import { useEffect, useState } from 'react'
import { apiClient } from '@/lib/api-client'
import { Link } from 'react-router-dom'

interface Stats {
  totalUsers: number
  totalThreads: number
  totalPosts: number
  activeUsers: number
  reportsCount: number
}

export function AdminDashboard() {
  const [stats, setStats] = useState<Stats | null>(null)
  const [isLoading, setIsLoading] = useState(true)

  useEffect(() => {
    fetchStats()
  }, [])

  const fetchStats = async () => {
    try {
      const response = await apiClient.get<Stats>('/admin/stats')
      setStats(response.data)
    } finally {
      setIsLoading(false)
    }
  }

  if (isLoading) return <div>Cargando...</div>

  return (
    <div className="container mx-auto px-4 py-8">
      <h1 className="text-3xl font-bold mb-8">Panel de Administración</h1>

      {/* Cuadrícula de Estadísticas */}
      <div className="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-5 gap-4 mb-8">
        <StatCard label="Usuarios Totales" value={stats.totalUsers} icon="👥" />
        <StatCard label="Hilos Totales" value={stats.totalThreads} icon="💬" />
        <StatCard label="Posts Totales" value={stats.totalPosts} icon="📝" />
        <StatCard label="Usuarios Activos (24h)" value={stats.activeUsers} icon="🟢" />
        <StatCard label="Reportes" value={stats.reportsCount} icon="⚠️" />
      </div>

      {/* Acciones Rápidas */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <QuickAction
          title="Gestionar Categorías"
          description="Crear, editar y organizar categorías del foro"
          link="/admin/categories"
          icon="📁"
        />
        <QuickAction
          title="Gestionar Usuarios"
          description="Ver usuarios, asignar roles y manejar baneos"
          link="/admin/users"
          icon="👥"
        />
        <QuickAction
          title="Cola de Moderación"
          description="Revisar contenido reportado y tomar acciones"
          link="/admin/moderation"
          icon="🛡️"
        />
      </div>
    </div>
  )
}

function StatCard({ label, value, icon }: { label: string; value: number; icon: string }) {
  return (
    <div className="p-4 bg-slate-900 rounded-lg border border-slate-800">
      <div className="text-2xl mb-2">{icon}</div>
      <p className="text-sm text-slate-400 mb-1">{label}</p>
      <p className="text-2xl font-bold text-slate-100">{value.toLocaleString()}</p>
    </div>
  )
}

function QuickAction({ title, description, link, icon }: {
  title: string
  description: string
  link: string
  icon: string
}) {
  return (
    <Link
      to={link}
      className="p-6 bg-slate-900 rounded-lg border border-slate-800 hover:border-blue-600 transition-colors"
    >
      <div className="text-3xl mb-3">{icon}</div>
      <h3 className="text-lg font-semibold text-slate-100 mb-2">{title}</h3>
      <p className="text-sm text-slate-400">{description}</p>
    </Link>
  )
}
```

---

## Estadísticas en Tiempo Real con WebSockets

```tsx
// src/pages/admin/AdminDashboard.tsx

import { useWebSocket } from '@/hooks/useWebSocket'
import { useState, useEffect } from 'react'

export function AdminDashboard() {
  const [stats, setStats] = useState<Stats | null>(null)
  
  // Actualizaciones en tiempo real vía WebSocket
  const { lastMessage } = useWebSocket('ws://localhost:3001/admin', {
    onMessage: (data) => {
      if (data.type === 'stats_update') {
        setStats(prev => ({ ...prev, ...data.stats }))
      }
    },
  })

  return (
    <div>
      <div className="flex items-center gap-2 mb-4">
        <div className="w-2 h-2 bg-green-500 rounded-full animate-pulse" />
        <span className="text-sm text-slate-400">Actualizaciones en vivo</span>
      </div>
      
      {/* Visualización de estadísticas */}
    </div>
  )
}
```

---

## Gráficos y Visualizaciones

```tsx
// src/components/admin/StatsChart.tsx

import { Line } from 'react-chartjs-2'
import { useState, useEffect } from 'react'

export function StatsChart() {
  const [data, setData] = useState({
    labels: [],
    datasets: [],
  })

  useEffect(() => {
    fetchChartData()
  }, [])

  const fetchChartData = async () => {
    const response = await apiClient.get('/admin/stats/chart')
    
    setData({
      labels: response.data.labels,
      datasets: [
        {
          label: 'Nuevos Usuarios',
          data: response.data.users,
          borderColor: 'rgb(59, 130, 246)',
          backgroundColor: 'rgba(59, 130, 246, 0.1)',
        },
        {
          label: 'Nuevos Hilos',
          data: response.data.threads,
          borderColor: 'rgb(34, 197, 94)',
          backgroundColor: 'rgba(34, 197, 94, 0.1)',
        },
      ],
    })
  }

  return (
    <div className="p-6 bg-slate-900 rounded-lg border border-slate-800">
      <h2 className="text-xl font-bold mb-4">Tendencia de Actividad (Últimos 7 Días)</h2>
      <Line data={data} options={{
        responsive: true,
        plugins: {
          legend: {
            position: 'top',
          },
        },
      }} />
    </div>
  )
}
```

---

## Feed de Actividad

```tsx
// src/components/admin/ActivityFeed.tsx

import { useState, useEffect } from 'react'
import { formatDistanceToNow } from 'date-fns'

interface Activity {
  id: string
  type: 'user_registered' | 'thread_created' | 'post_reported' | 'user_banned'
  user: { username: string; avatarUrl: string }
  createdAt: string
  metadata?: any
}

export function ActivityFeed() {
  const [activities, setActivities] = useState<Activity[]>([])

  useEffect(() => {
    fetchActivities()
    const interval = setInterval(fetchActivities, 30000) // Actualizar cada 30s
    return () => clearInterval(interval)
  }, [])

  const fetchActivities = async () => {
    const response = await apiClient.get('/admin/activities')
    setActivities(response.data)
  }

  return (
    <div className="p-6 bg-slate-900 rounded-lg border border-slate-800">
      <h2 className="text-xl font-bold mb-4">Actividad Reciente</h2>
      
      <div className="space-y-3">
        {activities.map(activity => (
          <div key={activity.id} className="flex items-start gap-3 p-3 bg-slate-800 rounded-lg">
            <img
              src={activity.user.avatarUrl}
              alt={activity.user.username}
              className="w-10 h-10 rounded-full"
            />
            <div className="flex-1">
              <p className="text-sm text-slate-300">
                <span className="font-medium">{activity.user.username}</span>
                {' '}
                {getActivityText(activity)}
              </p>
              <p className="text-xs text-slate-500 mt-1">
                {formatDistanceToNow(new Date(activity.createdAt), { addSuffix: true })}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  )
}

function getActivityText(activity: Activity): string {
  switch (activity.type) {
    case 'user_registered': return 'se registró'
    case 'thread_created': return `creó el hilo "${activity.metadata.title}"`
    case 'post_reported': return 'reportó un post'
    case 'user_banned': return 'fue baneado'
    default: return 'realizó una acción'
  }
}
```

---

## Registros de Auditoría

```tsx
// src/components/admin/AuditLogs.tsx

export function AuditLogs() {
  const [logs, setLogs] = useState([])
  const [filters, setFilters] = useState({
    action: 'all',
    dateRange: 'today',
  })

  return (
    <div className="p-6 bg-slate-900 rounded-lg border border-slate-800">
      <div className="flex items-center justify-between mb-4">
        <h2 className="text-xl font-bold">Registros de Auditoría</h2>
        
        <div className="flex gap-2">
          <select
            value={filters.action}
            onChange={(e) => setFilters({ ...filters, action: e.target.value })}
            className="px-3 py-2 bg-slate-800 border border-slate-700 rounded-lg text-sm"
          >
            <option value="all">Todas las Acciones</option>
            <option value="user_banned">Baneos de Usuario</option>
            <option value="post_deleted">Eliminaciones de Post</option>
            <option value="role_changed">Cambios de Rol</option>
          </select>
          
          <button className="px-4 py-2 bg-blue-600 text-white rounded-lg text-sm">
            Exportar CSV
          </button>
        </div>
      </div>

      <div className="overflow-x-auto">
        <table className="w-full">
          <thead>
            <tr className="border-b border-slate-800">
              <th className="text-left py-3 px-4 text-sm font-medium text-slate-400">Hora</th>
              <th className="text-left py-3 px-4 text-sm font-medium text-slate-400">Admin</th>
              <th className="text-left py-3 px-4 text-sm font-medium text-slate-400">Acción</th>
              <th className="text-left py-3 px-4 text-sm font-medium text-slate-400">Objetivo</th>
            </tr>
          </thead>
          <tbody>
            {logs.map(log => (
              <tr key={log.id} className="border-b border-slate-800">
                <td className="py-3 px-4 text-sm">{formatDate(log.createdAt)}</td>
                <td className="py-3 px-4 text-sm">{log.admin.username}</td>
                <td className="py-3 px-4 text-sm">{log.action}</td>
                <td className="py-3 px-4 text-sm">{log.target}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  )
}
```

---

## Resumen

### Características Clave

1. **Estadísticas en Tiempo Real**: Actualizaciones en vivo vía WebSockets
2. **Gráficos**: Tendencias visuales y analíticas
3. **Feed de Actividad**: Actividad reciente del foro
4. **Acciones Rápidas**: Acceso rápido a herramientas de administración
5. **Registros de Auditoría**: Rastrear todas las acciones de administración

---

Siguiente: [Gestión de Usuarios →](/es/react/8.admin-module/2.user-management)
