---
title: Footer, Breadcrumbs & Helpers de Navegación
description: Completa tu app shell con footer, navegación de breadcrumbs y patrones de accesibilidad. Aprende HTML semántico y mejores prácticas de navegación.
---

# Disclaimer

:::u-alert
---
title: Los comentarios en el código son solo educativos.
description: En proyectos reales, evita comentarios irrelevantes o redundantes.
color: warning
variant: subtle
icon: i-lucide-triangle-alert
---
:::

Completamos el app shell con footer y breadcrumbs - esenciales para navegación y SEO.

**Prerrequisitos**: Asegúrate de haber leído [App Shell](/es/react/3.layout/1.app-shell) primero, que cubre fundamentos de React como `useLocation`, `Link` y composición de componentes.

---

## Componente Footer

```tsx
// src/components/layout/Footer.tsx

import { Link } from 'react-router-dom'

/**
 * Footer - Parte inferior de cada página
 * 
 * Contiene:
 * - Logo y tagline
 * - Enlaces rápidos (navegación)
 * - Enlaces de redes sociales
 * - Aviso de copyright
 * 
 * HTML semántico:
 * - <footer> para footer de página
 * - <nav> para secciones de navegación
 * - Jerarquía de encabezados adecuada
 */
export function Footer() {
  return (
    <footer className="border-t border-slate-800 bg-slate-900 mt-auto">
      <div className="container mx-auto px-4 py-12">
        <div className="grid grid-cols-1 md:grid-cols-4 gap-8">
          {/* Columna de Marca */}
          <div className="col-span-1 md:col-span-2">
            <div className="flex items-center gap-2 mb-4">
              <div className="text-2xl">💬</div>
              <span className="text-xl font-bold text-slate-100">Forum</span>
            </div>
            <p className="text-slate-400 text-sm mb-4 max-w-md">
              Una plataforma de foro moderna construida con React. Conecta con otros, comparte conocimiento y participa en discusiones significativas.
            </p>
            {/* Enlaces Sociales */}
            <div className="flex items-center gap-4">
              <SocialLink href="https://twitter.com" icon="🐦" label="Twitter" />
              <SocialLink href="https://github.com" icon="🐙" label="GitHub" />
              <SocialLink href="https://discord.com" icon="💬" label="Discord" />
            </div>
          </div>

          {/* Enlaces Rápidos */}
          <div>
            <h3 className="text-sm font-semibold text-slate-100 uppercase tracking-wider mb-4">
              Enlaces Rápidos
            </h3>
            <nav className="space-y-2">
              <FooterLink to="/about">Acerca de</FooterLink>
              <FooterLink to="/faq">FAQ</FooterLink>
              <FooterLink to="/guidelines">Directrices de la Comunidad</FooterLink>
              <FooterLink to="/privacy">Política de Privacidad</FooterLink>
              <FooterLink to="/terms">Términos de Servicio</FooterLink>
            </nav>
          </div>

          {/* Recursos */}
          <div>
            <h3 className="text-sm font-semibold text-slate-100 uppercase tracking-wider mb-4">
              Recursos
            </h3>
            <nav className="space-y-2">
              <FooterLink to="/docs">Documentación</FooterLink>
              <FooterLink to="/api">API</FooterLink>
              <FooterLink to="/support">Soporte</FooterLink>
              <FooterLink to="/blog">Blog</FooterLink>
            </nav>
          </div>
        </div>

        {/* Barra Inferior */}
        <div className="mt-12 pt-8 border-t border-slate-800 flex flex-col md:flex-row justify-between items-center gap-4">
          <p className="text-sm text-slate-500">
            © {new Date().getFullYear()} Forum. Todos los derechos reservados.
          </p>
          <div className="flex items-center gap-4 text-sm">
            <button className="text-slate-400 hover:text-slate-200">
              Español
            </button>
            <span className="text-slate-700">|</span>
            <button className="text-slate-400 hover:text-slate-200">
              🌙 Oscuro
            </button>
          </div>
        </div>
      </div>
    </footer>
  )
}

function FooterLink({ to, children }: { to: string; children: React.ReactNode }) {
  return (
    <Link
      to={to}
      className="block text-sm text-slate-400 hover:text-slate-200 transition-colors"
    >
      {children}
    </Link>
  )
}

function SocialLink({ href, icon, label }: { href: string; icon: string; label: string }) {
  return (
    <a
      href={href}
      target="_blank"
      rel="noopener noreferrer"
      aria-label={label}
      className="text-2xl hover:scale-110 transition-transform"
    >
      {icon}
    </a>
  )
}
```

---

## Componente Breadcrumbs

Los breadcrumbs muestran la ubicación del usuario en la jerarquía del sitio:

```
Inicio > Categorías > Soporte Técnico > ¿Cómo aprender React?
```

```tsx
// src/components/layout/Breadcrumbs.tsx

import { Link } from 'react-router-dom'

export interface Breadcrumb {
  label: string
  href: string
}

interface BreadcrumbsProps {
  items: Breadcrumb[]
}

/**
 * Breadcrumbs - Mostrar ruta de navegación
 * 
 * Ayuda a los usuarios a entender su ubicación en el sitio.
 * Mejora el SEO (los motores de búsqueda aman los breadcrumbs).
 * 
 * HTML semántico:
 * - <nav> con aria-label="Breadcrumb"
 * - <ol> lista ordenada (jerárquica)
 * - Último item tiene aria-current="page"
 */
export function Breadcrumbs({ items }: BreadcrumbsProps) {
  return (
    <nav aria-label="Breadcrumb" className="mb-6">
      <ol className="flex items-center gap-2 text-sm">
        {/* Enlace Inicio (siempre primero) */}
        <BreadcrumbItem href="/" label="Inicio" />

        {/* Items dinámicos */}
        {items.map((item, index) => (
          <BreadcrumbItem
            key={item.href}
            href={item.href}
            label={item.label}
            isLast={index === items.length - 1}
          />
        ))}
      </ol>
    </nav>
  )
}

function BreadcrumbItem({
  href,
  label,
  isLast = false,
}: {
  href: string
  label: string
  isLast?: boolean
}) {
  return (
    <>
      <li>
        {isLast ? (
          /* Página actual: no es enlace */
          <span
            className="text-slate-100 font-medium"
            aria-current="page"
          >
            {label}
          </span>
        ) : (
          /* Páginas anteriores: enlaces */
          <Link
            to={href}
            className="text-slate-400 hover:text-slate-200 transition-colors"
          >
            {label}
          </Link>
        )}
      </li>

      {/* Separador */}
      {!isLast && (
        <li className="text-slate-600" aria-hidden="true">
          /
        </li>
      )}
    </>
  )
}
```

### Uso en Página de Detalle de Thread

```tsx
// src/pages/ThreadDetailPage.tsx

export function ThreadDetailPage() {
  const { slug } = useParams()
  const { thread } = useThread(slug)

  const breadcrumbs: Breadcrumb[] = [
    { label: 'Categorías', href: '/categories' },
    { label: thread.category.name, href: `/categories/${thread.category.slug}` },
    { label: thread.title, href: `/threads/${thread.slug}` },
  ]

  return (
    <div className="container mx-auto px-4 py-8">
      <Breadcrumbs items={breadcrumbs} />
      {/* Contenido del thread */}
    </div>
  )
}
```

---

## Componente Back Button

Helper de navegación simple:

```tsx
// src/components/ui/BackButton.tsx

import { useNavigate } from 'react-router-dom'

interface BackButtonProps {
  fallback?: string  // URL de fallback si no hay historial
  label?: string
}

/**
 * BackButton - Navegar a página anterior
 * 
 * Usa el historial del navegador cuando está disponible.
 * Retrocede a URL especificada si no hay historial.
 */
export function BackButton({ fallback = '/', label = 'Atrás' }: BackButtonProps) {
  const navigate = useNavigate()

  const handleClick = () => {
    // Verificar si hay historial
    if (window.history.length > 2) {
      navigate(-1)  // Ir atrás
    } else {
      navigate(fallback)  // Ir a fallback
    }
  }

  return (
    <button
      onClick={handleClick}
      className="flex items-center gap-2 text-slate-400 hover:text-slate-200 transition-colors"
    >
      <span>←</span>
      <span>{label}</span>
    </button>
  )
}
```

---

## Scroll to Top

Al navegar entre páginas, hacer scroll hacia arriba:

```tsx
// src/components/layout/ScrollToTop.tsx

import { useEffect } from 'react'
import { useLocation } from 'react-router-dom'

/**
 * ScrollToTop - Hacer scroll hacia arriba en cambio de ruta
 * 
 * Colocar en App.tsx dentro de BrowserRouter.
 * Hace scroll de la ventana hacia arriba cada vez que la ruta cambia.
 */
export function ScrollToTop() {
  const { pathname } = useLocation()

  useEffect(() => {
    window.scrollTo(0, 0)
  }, [pathname])

  return null  // No renderiza nada
}

// Uso en App.tsx:
<BrowserRouter>
  <ScrollToTop />
  <Routes>...</Routes>
</BrowserRouter>
```

---

## Skip to Content Link

Accesibilidad: Permitir que usuarios de teclado omitan navegación:

```tsx
// src/components/layout/SkipLink.tsx

/**
 * SkipLink - Saltar al contenido principal
 * 
 * Oculto por defecto, visible en foco de teclado.
 * Ayuda a usuarios de screen reader a omitir navegación.
 */
export function SkipLink() {
  return (
    <a
      href="#main-content"
      className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-blue-600 focus:text-white focus:rounded-lg"
    >
      Saltar al contenido principal
    </a>
  )
}

// En PublicLayout:
<div>
  <SkipLink />
  <Navbar />
  <main id="main-content">
    <Outlet />
  </main>
  <Footer />
</div>
```

---

## Progress Bar

Mostrar estado de carga durante navegación:

```tsx
// src/components/layout/ProgressBar.tsx

import { useEffect, useState } from 'react'
import { useNavigation } from 'react-router-dom'

/**
 * ProgressBar - Indicador de carga para navegación
 * 
 * Se muestra en la parte superior de la página durante navegación.
 * Usa el hook useNavigation de React Router.
 */
export function ProgressBar() {
  const navigation = useNavigation()
  const [progress, setProgress] = useState(0)

  useEffect(() => {
    if (navigation.state === 'loading') {
      // Simular progreso
      setProgress(10)
      const timer1 = setTimeout(() => setProgress(40), 100)
      const timer2 = setTimeout(() => setProgress(70), 300)
      
      return () => {
        clearTimeout(timer1)
        clearTimeout(timer2)
      }
    } else {
      setProgress(100)
      setTimeout(() => setProgress(0), 200)
    }
  }, [navigation.state])

  if (progress === 0) return null

  return (
    <div className="fixed top-0 left-0 right-0 h-1 bg-slate-800 z-50">
      <div
        className="h-full bg-blue-600 transition-all duration-300"
        style={{ width: `${progress}%` }}
      />
    </div>
  )
}
```

---

## Resumen

### Componentes Construidos

1. **Footer**: Branding, enlaces, copyright
2. **Breadcrumbs**: Ruta de navegación
3. **BackButton**: Navegación a página anterior
4. **ScrollToTop**: Auto-scroll en cambio de ruta
5. **SkipLink**: Accesibilidad para usuarios de teclado
6. **ProgressBar**: Indicador de carga

### Características de Accesibilidad

- ✅ HTML semántico (`<nav>`, `<footer>`, `<main>`)
- ✅ Etiquetas ARIA (`aria-label`, `aria-current`)
- ✅ Navegación por teclado (skip links)
- ✅ Indicadores de foco (`:focus-visible`)
- ✅ Amigable para screen readers

### Beneficios SEO

- ✅ Markup semántico
- ✅ Datos estructurados de breadcrumbs
- ✅ Texto de enlace descriptivo
- ✅ Jerarquía de encabezados adecuada

---

## ¡Módulo de Layout Completado! 🎉

Ahora tienes un app shell completo y listo para producción:
- ✅ Navbar responsiva con menú de usuario
- ✅ Sidebar consciente del contexto con filtros
- ✅ Footer con enlaces y branding
- ✅ Breadcrumbs para navegación
- ✅ Características de accesibilidad
- ✅ Diseño responsivo móvil

**Siguiente:** ¡Construye funcionalidades sobre esta base sólida!

Continúa con [Módulo de Categorías →](/es/react/4.categories-module/1.list-categories)
